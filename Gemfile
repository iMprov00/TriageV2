source 'https://rubygems.org'


gem 'sinatra'
gem 'sinatra-activerecord'
gem 'sqlite3'
gem 'rake'
gem 'puma'
gem 'require_all'
gem 'rackup' # ← ДОБАВЬТЕ ЭТУ СТРОЧКУ
gem 'wdm', '>= 0.1.0' # ← И ЭТУ СТРОЧКУ для Windows

# Для разработки
group :development do
  gem 'pry'
  gem 'rerun'
end

# Для тестирования
group :test do
  gem 'rspec'
  gem 'capybara'
end